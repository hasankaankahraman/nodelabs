import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF080908); // Ana renk (Background)
  static const Color accent = Color(0xFFE50914); // Vurgu rengi (Butonlar)
  static const Color textPrimary = Color(0xFFFEFEFF); // Ana yazı rengi (Beyaz)
  static const Color textSecondary = Color(0xFF848484); // Açık gri yazılar
  static const Color border = Color(0xFF4D4D4D); // Kenarlık rengi #4c4c4d olacak
  static const Color buttonBackground = Color(0xFFE50914); // Buton arka planı
  static const Color socialButtonBackground = Color(0xFF232322); // Sosyal buton rengi
}
